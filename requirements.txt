# requirements.txt
# Core scientific computing
numpy>=1.20.0

# GUI framework
PyQt6>=6.0.0

# Data handling
pandas>=1.3.0

# Plotting
matplotlib>=3.5.0

# 3D visualization (can be problematic on some systems)
# If vtk installation fails, try:
# conda install -c conda-forge vtk
# or system package manager
vtk>=9.0.0

# Alternative installation command:
# pip install -r requirements.txt
# If VTK fails, comment out the vtk line above and install separately
